events {
    worker_connections 1024;
}

http {
    upstream dashboard {
        server 127.0.0.1:3000;
    }

    upstream metrics {
        server 127.0.0.1:2112;
    }

    upstream sqs {
        server 127.0.0.1:3001;
    }

    server {
        listen PORT_PLACEHOLDER;
        server_name _;

        # Prometheus metrics
        location /metrics {
            proxy_pass http://metrics;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Dashboard specific POST endpoints
        location ~ ^/queues(/.*)?$ {
            proxy_pass http://dashboard;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_pass_request_headers on;
        }

        # SQS API endpoints (all other requests)
        location / {
            # Check if it's a dashboard GET request to root
            if ($request_method = GET) {
                proxy_pass http://dashboard;
            }
            # All other requests (including POST to root) go to SQS
            proxy_pass http://sqs;
            proxy_read_timeout 600s;
            proxy_send_timeout 600s;
            # Preserve original Host header for AWS signature validation
            proxy_set_header Host $http_host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            # Ensure all headers are forwarded to SQS for AWS authentication
            proxy_pass_request_headers on;
            proxy_set_header Authorization $http_authorization;
            proxy_set_header X-Amz-Date $http_x_amz_date;
            proxy_set_header X-Amz-Target $http_x_amz_target;
            proxy_set_header X-Amz-Content-Sha256 $http_x_amz_content_sha256;
        }
    }
}
